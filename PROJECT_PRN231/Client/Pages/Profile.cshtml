@page
@model Client.Pages.ProfileModel
@{
    Layout = "Areas/Views/Shared/_Layout.cshtml";
}
<div class="container rounded bg-white mt-5 mb-5">
    <div class="row">
        <div class="col-md-3 border-right">
            @if (String.IsNullOrEmpty(Model.user.Image))
            {
                <div class="d-flex flex-column align-items-center text-center p-3 py-5"><img id="imgPreview" class="rounded-circle mt-5" width="150px" height="150px" src="~/Image/6d4617dd0c9e91a.png"></div>
            }
            else
            {
                <div class="d-flex flex-column align-items-center text-center p-3 py-5"><img id="imgPreview" class="rounded-circle mt-5" width="150px" height="150px" src="~/Image/ContactImages/@Model.user.Image"></div>
            }
        </div>
        <div class="col-md-5 border-right">
            <div class="p-3 py-5">
                <div class="d-flex justify-content-between align-items-center mb-3 mt-4">
                    <h4 class="text-right">User Profile</h4>
                    @if (TempData["success"] != null)
                    {
                        <div class="alert alert-success">
                            <strong>Success!</strong> @TempData["success"]
                        </div>
                    }
                    @if (ViewData["ErrorUpdate"] != null)
                    {
                        <div class="alert alert-danger">
                            <strong>Fail!</strong> @ViewData["ErrorUpdate"]
                        </div>
                    }
                </div>
                @if (ViewData["Error"] != null)
                {
                    <div class="alert alert-danger">
                        <strong>Fail!</strong> @ViewData["Error"]
                    </div>
                }
                else
                {
                    <form method="post" enctype="multipart/form-data">
                        <input type="hidden" asp-for="user.Id" />
                        <input type="hidden" asp-for="user.Password" />
                        <div class="row mt-2">
                            <div class="col-md-12"><label class="labels">Image</label><input type="file" class="form-control" id="chooseImg" accept="Image/*" name="fThumb"><span class="text-danger"></span></div>
                            <div class="col-md-6"><label class="labels">Full Name</label><input type="text" class="form-control" placeholder="@Model.user.Name" asp-for="user.Name"><span asp-validation-for="user.Name" class="text-danger"></span></div>
                            <div class="col-md-6"><label class="labels">Username</label><input type="text" class="form-control" placeholder="@Model.user.Username" asp-for="user.Username" readonly><span asp-validation-for="user.Username" class="text-danger"></span></div>
                        </div>
                        <div class="row mt-3">
                            <div class="col-md-12"><label class="labels">Mobile Number</label><input type="text" class="form-control" placeholder="@Model.user.Phone" asp-for="user.Phone"><span asp-validation-for="user.Phone" class="text-danger"></span></div>
                            <div class="col-md-12"><label class="labels">Email</label><input type="text" class="form-control" placeholder="@Model.user.Email" asp-for="user.Email"><span asp-validation-for="user.Email" class="text-danger"></span></div>
                            <div class="col-md-12"><label class="labels">Address</label><input type="text" class="form-control" placeholder="@Model.user.Adress" asp-for="user.Adress"><span asp-validation-for="user.Adress" class="text-danger"></span></div>
                        </div>
                        <div class="mt-5 text-center"><button class="btn btn-primary profile-button" type="submit">Save Profile</button></div>
                    </form>
                }                
            </div>
        </div>
    </div>
</div>